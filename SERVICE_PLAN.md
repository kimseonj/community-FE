# 종주메이트 서비스 기획

## 한 줄 정의

국토종주 이용자가 진행 중인 여정과 최종 완주 기록을 공유하고, 자주 바뀌는 코스 상태를 제보·구독해 알림받는 커뮤니티 서비스다.

## 해결하려는 문제

- 종주 중인 사람은 현재 상황을 다른 이용자와 나눌 곳이 필요하다.
- 완주자는 사진과 최종 기록을 남기고 다른 이용자의 준비에 도움을 줄 수 있다.
- 국토종주 코스는 공사·통제·우회가 잦지만 변경 정보를 제때 찾기 어렵다.
- 관심 코스의 상태가 바뀌었는지 매번 직접 확인하기 어렵다.

## 핵심 사용자 흐름

1. 이용자는 전체 기록에서 다른 사람의 진행 상황과 완주 기록을 본다.
2. 로그인한 이용자는 `진행 중` 또는 `완주` 상태로 글과 사진을 공유한다.
3. 이용자는 코스별 현재 상태를 확인하고 관심 코스를 구독한다.
4. 현장 상황을 본 이용자는 정상·주의·공사·통제로 상태를 제보한다.
5. 구독자는 새 제보가 등록되면 인앱 알림을 받고 내용을 확인한다.

## 현재 프론트·백엔드 구현 범위

| 기능 | 상태 | 연결 API |
| --- | --- | --- |
| 회원가입·로그인·프로필 | 연결됨 | `/users`, `/auth`, `/users/{id}` |
| 기록 목록·상세·작성·수정·삭제 | 연결됨 | `/posts`, `/posts/{id}` |
| 댓글·좋아요 | 연결됨 | `/posts/{id}/comments`, `/posts/{id}/likes` |
| 코스 목록·현재 상태 | 연결됨 | `/courses` |
| 코스 구독·해제 | 연결됨 | `/courses/subscriptions`, `/courses/{id}/subscription` |
| 코스 상태 제보 | 연결됨 | `/courses/{id}/reports` |
| 알림 목록·읽음 처리 | 연결됨 | `/me/notifications` |
| 관리자 제보 사후 관리 화면 | 프론트 구현, 백엔드 API 없음 | `/admin/course-reports` 필요 |
| 코스별 제보 상세·이력 | 백엔드 API 없음 | 추가 필요 |
| 거리·구간·진행률이 포함된 종주 기록 | 백엔드 데이터 모델 없음 | 추가 필요 |

## 프론트 원칙

- 백엔드에 없는 거리, 진행률, 체크포인트, 제보 상세는 가짜 데이터로 표시하지 않는다.
- 데이터가 없으면 빈 상태와 다음 행동만 보여준다.
- 하단 주요 메뉴는 홈·기록·코스·내 정보 네 개로 유지한다.
- 기록 작성은 어느 화면에서든 접근 가능한 플로팅 버튼으로 제공한다.
- 코스 구독 알림은 현재 구현된 인앱 알림을 기준으로 한다.

## 백엔드에 추가하면 좋은 항목

### 1. 실제 국토종주 코스 기본 데이터

현재 실행 DB에는 부하테스트용 코스명이 들어 있다. 서비스 운영 전 실제 코스명과 식별자를 초기 데이터로 관리하고 테스트 데이터는 분리해야 한다.

### 2. 코스 제보 조회 API

`GET /courses/{courseId}/reports`가 필요하다. 최소 응답은 제보 타입, 내용, 작성 시각이다. 위치, 사진, 공사 예상 종료일은 실제 요구가 생긴 뒤 추가한다.

### 3. 구조화된 종주 진행 기록

현재 게시글은 상태·제목·본문·사진만 저장한다. 거리, 출발일, 완주일, 선택 코스, 현재 구간, 진행률을 검색·집계하려면 게시글과 별도의 종주 기록 모델 또는 게시글 확장이 필요하다. 그 전까지 프론트는 본문으로 상황을 공유한다.

### 4. 내 기록 조회 API

프로필에서 안정적으로 내 기록만 보여주려면 닉네임 필터 대신 사용자 ID 기반 `GET /me/posts`가 필요하다. 닉네임은 변경될 수 있으므로 사용자 식별자로 쓰면 안 된다.

### 5. 푸시 알림은 후순위

현재는 앱을 열었을 때 확인하는 인앱 알림이다. 앱을 닫은 상태에서도 알림이 필요해질 때 웹 푸시 구독 정보와 발송 처리를 추가한다.

### 6. 관리자 제보 관리 API

제보는 등록 즉시 코스 상태에 반영하고, 관리자는 잘못된 제보만 사후에 수정·삭제한다. 프론트는 관리자 계정의 `role`을 확인해 `/admin/reports` 화면을 노출한다. 실제 관리를 위해 다음 API와 응답 필드가 필요하다.

- `GET /admin/course-reports?cursor={id}&size=20`: 제보 목록과 `nextCursor`, `hasNext` 반환
- `PATCH /admin/course-reports/{id}`: 타입·내용 수정
- `DELETE /admin/course-reports/{id}`: 제보 소프트 삭제
- 목록 항목: `id`, `courseId`, `courseName`, `reporterNickname`, `type`, `content`, `createdAt`

수정·삭제한 제보가 해당 코스의 최신 제보라면 백엔드는 코스의 현재 상태를 다시 계산해야 한다. 이미 발송된 알림은 회수하지 않는다. 모든 `/admin/**` API는 프론트의 역할 표시와 별개로 서버에서 `ADMIN` 권한을 검증해야 한다. 공개 회원가입 요청의 `role`도 서버가 항상 `USER`로 강제해야 관리자 계정 생성 취약점을 막을 수 있다.

## 이번 범위에서 제외

- 지도 경로 표시와 GPS 추적
- 랭킹·배지·포인트
- 웹 푸시

핵심 흐름의 실제 사용 데이터를 확인한 뒤 필요한 기능만 추가한다.
